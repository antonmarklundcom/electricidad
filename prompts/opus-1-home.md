# Phase O1 — Homepage. Opus session. Lane 1.

Read ONLY: this file, `plan.md` §1, §2, §4, §5.2, the phase table, §9, and `docs/log/O0.md`.
Execute under the autonomy protocol §4. Build nothing outside the plan.

Owns: `index.php`, new `partials/home-*.php`, `content/home.php` (new), `assets/img/favicon.svg`,
`assets/img/og-default.png`, a `/* == O1 == */` block at the END of `assets/css/site.css`,
`docs/log/O1.md`. Not the tokens block, not `partials/header.php` / `footer.php`, not `lib/**`.

Budget: one session, ≤ 90 min. When the exit criteria pass, open the PR that turn (§4.13).

Phase rules:
- Branch `phase/O1` off latest main. WIP commit every 30 min.
- Load `php-site-template` (partial scope rules, `e()` escaping) and `vendercrm-lead-capture`
  (the form is already wired; do not touch `enviar.php`).
- Sections and copy per §5.2. Service cards are DERIVED from `content/services.php` by
  cluster — never a hand-written list. Calculator teaser renders a card only for slugs that
  exist in `content/tools.php` (O2 adds them later), so the page is green now and gains cards
  by itself.
- Every partial prefixes its locals and `unset()`s them; every value through `e()`.
- Hero: WhatsApp CTA falls back to `/contacto/` while `site.whatsapp` is null (template does
  this — verify, do not reimplement). No invented phone, address, years, counts.
- OG image and favicon generated by a script (ImageMagick or PHP GD), text and brand colours
  only. No Higgsfield, no stock photos (§1.10).
- ONE Lighthouse run on `/` mobile after the last code change: performance ≥ 90, SEO ≥ 95,
  accessibility ≥ 95. Fix, do not re-run in a loop.

Exit: `/` renders every §5.2 section; FAQPage + LocalBusiness/Electrician JSON-LD validate
(Google rich results test or `deploy/` schema check); form posts in degraded mode; Lighthouse
numbers above met; `./verify.sh` green; PR merged green. Screenshots: CI artifact.

## After this phase
Follow `prompts/_handoff.md`. Next: `prompts/opus-2-tools.md`, model Opus (`claude-opus-5`).
Same model → continue in this window if `create_session` is unavailable.
